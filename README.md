# Trainable SQL Query Builder

Telegram бот для конвертации русскоязычных запросов в SQL с возможностью обучения на исправлениях.

## Особенности
- 📝 Конвертация естественного языка (русский) в SQL
- 🧠 Самообучение на исправлениях пользователей
- 💾 Умный кэш запросов и паттернов
- 🔍 Автоопределение схемы БД
- 🤖 Поддержка разных LLM провайдеров (Ollama, OpenAI)
- 📊 Статистика использования и стоимости
- ✅ Прямое редактирование SQL аналитиками

## Установка

1. Клонируйте репозиторий
2. Установите зависимости:
   ```bash
   pip install -r requirements.txt
Настройте переменные окружения:

bash
cp .env.example .env
# Отредактируйте .env файл
Настройте базу данных PostgreSQL

Запустите бота:

bash
python main.py
Использование
Начните диалог с ботом в Telegram

Задайте вопрос на русском языке:

"Сколько всего видео?"

"Статистика за ноябрь 2025"

"Прирост просмотров 28 ноября"

Выберите действие:

✅ Подтвердить и выполнить

✏️ Исправить SQL вручную

🔄 Перефразировать вопрос

Архитектура
text
Trainable SQL Query Builder/
├── bot/              # Telegram бот
├── constructor/      # Конструктор SQL с обучением
├── llm/             # Клиенты LLM и обучение на правках
├── database/        # Работа с БД и схемой
└── utils/           # Утилиты и логирование
Обучение системы
Система обучается автоматически:

Сохраняет успешные запросы в кэш

Анализирует исправления пользователей

Извлекает паттерны из исправлений

Применяет выученные правила к новым запросам

text

## Инструкция по запуску:

1. **Установите зависимости:**
```bash
pip install -r requirements.txt
Настройте переменные окружения:

bash
cp .env.example .env
# Отредактируйте .env файл
Настройте базу данных PostgreSQL:

sql
CREATE DATABASE video_analytics;
Запустите Ollama (если используете):

bash
ollama run llama3.2:3b
Запустите бота:

bash
python main.py
Начните общение в Telegram с вашим ботом.
